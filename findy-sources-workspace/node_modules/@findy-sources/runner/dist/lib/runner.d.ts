import { SourceDefinition } from '@findy-sources/meta';
declare const _default: (sources: SourceDefinition[]) => (rawProfile: any) => (rawOptions: any) => {
    name: string;
    iterate: any;
}[];
export default _default;
