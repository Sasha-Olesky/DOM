import { AnyItemDoNotUseInParse, Fail, Options, Profile } from '@findy-sources/meta';
import { AnyItem } from '@findy-sources/meta/dist/item/publicItemTypes';
export interface IterationContext {
    iteratorName: string;
    profile: Profile;
    options: Options;
    items: Partial<AnyItemDoNotUseInParse>[];
}
export interface ItemTest {
    message: () => string;
    /**
     * Value to examine
     */
    subject: (item: AnyItemDoNotUseInParse) => any;
    /**
     * Test if item is good
     */
    test: (item: AnyItemDoNotUseInParse) => boolean;
}
declare function filterItems(ic: IterationContext): {
    values: AnyItem[];
    discards: {
        item: AnyItemDoNotUseInParse;
        fails: Fail[];
    }[];
    warnings: {
        item: AnyItemDoNotUseInParse;
        fails: Fail[];
    }[];
};
export default filterItems;
