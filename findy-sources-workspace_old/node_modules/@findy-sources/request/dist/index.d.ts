import { NativeFetch } from '@findy-sources/meta';
export declare type RequestParams = Record<string, any>;
export declare type Request = (params: RequestParams, stringifyOptions?: {
    encode?: boolean;
    order?: string[];
}) => Promise<string>;
/**
 * Creates request function.
 *
 * const request = createRequest()
 * const html = await request({
 *   _origin: 'https://ya.ru',
 *   _path: '/search',
 *   q: 'bmw'
 * })
 *
 * @param nativeFetch function that implements NativeFetch interface
 * @param gzip enable adding of gzip headers to requests
 */
export declare const createRequest: (nativeFetch: NativeFetch, gzip: boolean) => Request;
